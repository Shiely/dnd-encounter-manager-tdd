[pytest]
# UI tests are ignored by default for speed (they are Qt-heavy).
# Run them with:   uv run pytest tests/unit/ui/ -q
# They are forced to run headless by default (no windows pop up).
# See README.md → "Running Tests" for Windows + full instructions.
addopts = --ignore=tests/unit/ui
